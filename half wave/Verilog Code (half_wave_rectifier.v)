module half_wave_rectifier(
    input wire clk,
    input wire signed [7:0] vin,
    output reg signed [7:0] vout
);

always @(posedge clk)
begin
    if(vin > 0)
        vout <= vin;
    else
        vout <= 0;
end

endmodule